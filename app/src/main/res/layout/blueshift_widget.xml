<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="0dp"
    android:background="@color/widget_background">

    <!-- Header with buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:background="@color/widget_surface">
        
        <ImageButton
            android:id="@+id/play_pause_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/button_background"
            android:src="@drawable/ic_play"
            android:contentDescription="Play/Pause"
            android:scaleType="fitCenter" />
        
        <!-- Player selector group -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="40dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/player_selector_background"
            android:paddingStart="4dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp">
            
            <ImageButton
                android:id="@+id/switch_player_button"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:padding="6dp"
                android:layout_marginEnd="4dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_audio_video_receiver"
                android:contentDescription="Switch player"
                android:scaleType="fitCenter" />
            
            <TextView
                android:id="@+id/player_indicator"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="No player selected"
                android:textSize="14sp"
                android:textColor="@color/widget_text_secondary"
                android:paddingStart="4dp"
                android:paddingEnd="4dp" />
        </LinearLayout>
        
        <ImageButton
            android:id="@+id/settings_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/button_background"
            android:src="@drawable/ic_settings"
            android:contentDescription="@string/settings"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <!-- Now playing information -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="4dp"
        android:paddingBottom="0dp"
        android:background="@color/widget_surface">
        
        <TextView
            android:id="@+id/now_playing"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:paddingBottom="16dp"
            android:text="-"
            android:textSize="13sp"
            android:textColor="@color/widget_text_primary"
            android:ellipsize="end"
            android:maxLines="3" />
        
        <!-- Visual separator using TextView -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginStart="-16dp"
            android:layout_marginEnd="-16dp"
            android:background="@color/widget_divider" />
    </LinearLayout>

    <!-- Station List (using ListView for dynamic content) -->
    <ListView
        android:id="@+id/stations_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:background="@color/widget_background" />

</LinearLayout>
